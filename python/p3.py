# Accept a number as input , say X and define a logic to get the ouyput say Y . The Input can be only 0 or 1 and the output must be 1 if imput is 0 and viceversa
# 

X=int(input("Enter the input number (0 or 1 only) : "))

Y=1-X
print(f'Input number={X} ,Output number={Y}')